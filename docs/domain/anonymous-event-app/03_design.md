# 匿名イベントアプリ — 詳細要件定義

対象は [01_requirements.md](./01_requirements.md) で確定した全要件。コンテキストは会員 `member`／イベント `event`／店 `place` の 3 つ（[02_analysis.md](./02_analysis.md)）。追加するエンティティは次の 7 つ。

| エンティティ | テーブル | コンテキスト | 役割 |
|---|---|---|---|
| `Member` | `member` | member | 会員（ログインする本人） |
| `MemberPassword` | `member_password` | member | 認証情報 |
| `MemberSession` | `member_session` | member | ログインセッション |
| `Event` | `event` | event | イベント。匿名で立ち、成立・終了までを持つ |
| `EventEntry` | `event_entry` | event | 参加。誰がどのイベントに押したか |
| `Place` | `place` | place | 店。Google の店への参照＋手動登録 |
| `PlaceReview` | `place_review` | place | レビュー。星と一言・実名 |

実装メモ: 認証まわり（Member / MemberPassword / MemberSession）は、雛形の認証コード（Customer 系）をリネームして流用する（[naming.md](./naming.md)）。設計上の扱いは他と同じく、このアプリ自身のエンティティである。

## 用語（ユビキタス言語）

サービス全体で使う呼称をここで 1 つに決める。会議でも仕様書でもコードでも、この呼称だけを使い、言い換えない。

| 呼称（これで統一する） | 何を指すか | 言い換えない |
|---|---|---|
| イベント | 匿名で立ち、成立・終了までを含む 1 件 | 「募集」「企画」 |
| 立案者 | イベントを立てた本人（ずっと匿名） | 「主催者」「ホスト」「オーナー」 |
| 参加 | 会員がイベントに押した 1 件 | 「エントリー」「申込」 |
| 参加者 | 参加を押している会員（立案者を含む） | 「メンバー」「出席者」 |
| 会員 | ログインする本人 | 「社員」「アカウント」「ユーザー」 |
| 集合日時 | 実際に集まる日時。過ぎたら終了 | 「開催日時」「開始時刻」「集合日時」 |
| 締切 | **成立判定を打ち切る期限**（既定＝集合日時）。成立後の参加受付は集合日時まで続くので「参加を受け付ける期限」ではない | 「応募期限」「エントリー期限」 |
| 成立人数 | 成立に必要な人数（立案者込み・2 以上） | 「定員」（上限の意味になる。定員は作らない） |
| 募集中 | 立ってから、成立か締切までの状態 | 「受付中」「オープン」 |
| 成立 | 参加が成立人数に達し、名前が開示された状態 | 「開催決定」「マッチング」 |
| 開示 | 成立した瞬間に、参加者の名前が全員に見えるようになること | 「公開」「発表」 |
| 終了 | 集合日時を過ぎた状態（当日中は一覧に残る） | 「開催済み」「クローズ」 |
| 不成立 | 締切までに人数が届かず終わったこと | 「流れた」「キャンセル」 |
| 取り消し | 立案者がイベントをやめること | 「取り下げ」「中止」 |
| 店 | レビューと提案の紐づけ先（社食・オフィス内の場所も含む） | 「お店」「店舗」「スポット」 |
| 提案 | 成立時にアプリが自動で出す店（3 件程度） | 「おすすめ」「レコメンド」 |
| レビュー | 星（5段階）と一言 | 「口コミ」「評価」 |
| 履歴 | 自分が立てた・押したイベントと自分のレビューの一覧（本人だけが見る） | 「アクティビティ」「マイページ」 |
| 表示名の読み | 表示名をカナで書いたもの（名簿を五十音順に並べるために持つ） | 「フリガナ」「ヨミガナ」 |

## ER 図

まず、データの繋がりだけを確認します。何が必須か・日時をどう持つかは、この図では決めません。

![匿名イベントアプリの ER 図。左に会員（Member）があり、会員 ID・公開用 UUID・メール・表示名とその読み（名簿の並び順に使う）・状態を持つ。その下に認証情報（MemberPassword）が 1 対 1、セッション（MemberSession）が 1 対多で繋がる。中央の参加（EventEntry）はイベント ID と会員 ID（成立まで誰にも見えない）を持ち、イベント × 会員の組で一意。会員とは 1 対多、右のイベント（Event）とは多対 1。イベントは公開用 ID・立案者の会員 ID（誰にも表示しない・点線の参照）・内容・集合日時・締切・成立人数・投稿先チャンネル・投稿の記録・成立日時・状態を持ち、店への参照は持たない。右下の店（Place）は Google の店への参照（手動登録なら無し）と店名（会員が付けた呼び名・誰でも直せる）を持つ。中央下のレビュー（PlaceReview）は店 ID・会員 ID（実名で出る）・星・一言を持ち、会員と店それぞれに 1 対多で繋がる。注記: この図で確認するのは「どこに何があって、いくつ繋がるか」だけ。イベントは店を知らず、place と member の世界もイベントを知らない。updatedAt / createdAt と各エンティティの管理 ID は省略](./img/er.svg)

- コンテキストをまたぐ参照は **ID だけ・一方向**（event → member、place → member）。**イベントは店を知らず**、place と member の世界もイベントを知らない
- `EventEntry` は会員とイベントの交差。**(eventId, memberId) の組で一意**

## EntityModel

形だけを書き、メソッドや検証は書かない。型では表せない決めごとは文章で添える。

### `Member` — 会員

```scala
case class Member(
  id:        Option[Id],                    // 管理 ID（永続化前は None）
  uuid:      UUID,                          // 公開用 UUID（連番を外に晒さない）
  email:     String,                        // ログイン ID（一意）
  name:      String,                        // 表示名（開示・レビューに出る）
  nameKana:  String,                        // 表示名の読み（名簿の並び順に使う）
  state:     Status        = Status.IS_ACTIVE, // アカウント状態
  updatedAt: LocalDateTime = Now,           // データ更新日
  createdAt: LocalDateTime = Now            // データ作成日
) extends EntityModel[Id]

object Member:

  /** 会員の識別子と状態 */
  type   Id = Id.Repr
  object Id extends Entity.Id[Long]

  type   UUID = UUID.Repr
  object UUID extends Entity.Id[String]

  /** アカウント状態 */
  enum Status(val code: Short) extends EnumStatus[Short]:
    case IS_INACTIVE extends Status(code = -1) // 無効
    case IS_ACTIVE   extends Status(code =  1) // 有効
```

**ここで型が語っていること**
- `uuid` が公開用、`id` は DB の中だけ。開示・レビュー表示の API レスポンスで会員を指す識別子として `uuid` を使い、連番を外に晒さない

**型では守れない決めごと**
- `email` は一意
- `name` は職場で誰だとわかる名前。**一意にしない**（同姓同名がありうる）。**変更できる**——名前を保存していないので、変更すると過去の名簿とレビューの表示も追従する
- `nameKana` は必須・全角カタカナのみ。ひらがなとの混在を許すと文字コード順で二群に割れて並びが壊れる。**正しい読みが入っていることは保証できない**（並びが読み順になるのは全員が正しく入れた場合だけ）
- 無効化（`IS_INACTIVE`）の操作・画面は作らない。問題発生時の DB 直接対応用の受け皿（→ [論点7](#論点7memberstatus-を残す)）

**保存されるデータの例**

| email | name | nameKana | state |
|---|---|---|---|
| ohira@example.co.jp | 大平 | オオヒラ | IS_ACTIVE |

### `MemberPassword` — 認証情報

```scala
case class MemberPassword(
  id:        Option[Id],                    // 管理 ID（永続化前は None）
  memberId:  Member.Id,                     // 会員 ID（1 会員に 1 行）
  hash:      String,                        // パスワードハッシュ
  updatedAt: LocalDateTime = Now,           // データ更新日
  createdAt: LocalDateTime = Now            // データ作成日
) extends EntityModel[Id]

object MemberPassword:

  /** 認証情報の識別子 */
  type   Id = Id.Repr
  object Id extends Entity.Id[Long]
```

**ここで型が語っていること**
- 他のエンティティと同じく自分の管理 `Id` を持つ（雛形の CustomerPassword と同じ形。ハッシュ化・照合の処理もコンパニオンが担う）

**型では守れない決めごと**
- `memberId` は一意——**1 会員に 1 行**
- 保存するのはハッシュだけ（平文・可逆な形は持たない）

**保存されるデータの例**

| memberId | hash |
|---|---|
| 10 | $pbkdf2$…（ハッシュ文字列） |

### `MemberSession` — セッション

```scala
case class MemberSession(
  id:        Option[Id],                    // 管理 ID（永続化前は None）
  memberId:  Member.Id,                     // 会員 ID
  token:     Token,                         // セッショントークン（一意・未署名。ixias の Token 型）
  expiresAt: LocalDateTime = Now.plusDays(30), // 有効期限
  updatedAt: LocalDateTime = Now,           // データ更新日
  createdAt: LocalDateTime = Now            // データ作成日
) extends EntityModel[Id]

object MemberSession:

  /** セッションの識別子 */
  type   Id = Id.Repr
  object Id extends Entity.Id[Long]
```

**ここで型が語っていること**
- 自分の `Id` を持つ＝会員と **1:***。スマホと PC で同時にログインできる

**型では守れない決めごと**
- `token` は一意。ログアウトは行の削除（即時失効）。`expiresAt` を過ぎたトークンは無効
- 雛形の CustomerSession にある `state`（IS_CLOSED / IS_ACTIVE）は持たない。ログアウトが行の削除である以上「有効なセッション＝行が在る」の一義で足り、state を足すと同じ意味が 2 通りに書けてしまう（雛形でも IS_CLOSED は定義だけで読み書きする処理がない）。代償として流用時に列を 1 本落とす

**保存されるデータの例**

| memberId | token | expiresAt |
|---|---|---|
| 10 | a1b2…（ランダム文字列） | 9/23 12:00 |

### `Event` — イベント

```scala
case class Event(
  id:             Option[Id],                      // 管理 ID（永続化前は None）
  code:           Code,                            // 公開用 ID（URL に使う。一意・推測できない）
  memberId:       Member.Id,                       // 立案者（誰にも表示しない）
  title:          String,                          // 内容（「今日ランチ行きたい」）
  startAt:        LocalDateTime,                   // 集合日時（イベントの始まり。過ぎたら終了）
  closeAt:        LocalDateTime,                   // 締切（既定＝集合日時。現在 < closeAt ≦ startAt）
  minEntries:     Short,                           // 成立人数（立案者込み・2〜50。達したら成立）
  slackChannelId: String,                          // 投稿先チャンネル（立案時に選ぶ）
  slackMessageId: Option[String],                  // bot 投稿の記録（投稿前は None）
  confirmedAt:    Option[LocalDateTime],           // 成立した日時（成立前は None）
  state:          Status        = Status.IS_OPEN,  // イベントの状態
  updatedAt:      LocalDateTime = Now,             // データ更新日
  createdAt:      LocalDateTime = Now              // データ作成日
) extends EntityModel[Id]

object Event:

  /** イベントの識別子 */
  type   Id = Id.Repr
  object Id extends Entity.Id[Long]

  /** 公開用 ID（URL 用） */
  type   Code = Code.Repr
  object Code extends Entity.Id[String]

  /** イベントの状態 */
  enum Status(val code: Short) extends EnumStatus[Short]:
    case IS_CANCELED extends Status(code = -2) // 取り消し: 立案者がやめた
    case IS_FAILED   extends Status(code = -1) // 不成立: 締切までに人数が届かなかった
    case IS_OPEN     extends Status(code =  1) // 募集中
    case IS_CONFIRMED extends Status(code = 2) // 成立: 名前が開示されている
    case IS_FINISHED extends Status(code =  3) // 終了: 集合日時を過ぎた
```

**ここで型が語っていること**
- `slackMessageId` が `Option` なのは、**まだ投稿していない状態が存在する**から（教材の `Payment.transactionId` と同型）
- `confirmedAt` が `Option` なのは、成立しないまま終わるイベントがあるから。**成立後に取り消されたかどうか**は `state = IS_CANCELED` かつ `confirmedAt` が `Some` で読む（→ [論点1](#論点1取り消しを-1-つの状態で表しcode成立前後の違いは-confirmedat-で読む)）
- 立案者の匿名はデータでは守らない。`memberId` は普通に持ち、**表示しない**だけ（アクセス境界の話）

**型では守れない決めごと**
- `code` は**一意**。`email` や `token` と同じく一意制約をかける。ログイン不要で詳細が見える唯一の入場鍵なので、推測できると募集中イベントを総当たりで列挙でき、最小表示のルールを守っても「誰かの誘いが流れた」の非可視化が崩れる。Slack に URL が流れ続けるので**後から変えられない**
- `現在 < closeAt ≦ startAt`。過去の `startAt` では作れない（下限が無いと「昨日締切・明日集合」の参加できないイベントが作れる）
- `minEntries` は 2 以上 50 以下
- `state` と `confirmedAt` は連動する: `IS_CONFIRMED`・`IS_FINISHED` なら必ず `Some`、`IS_OPEN`・`IS_FAILED` なら必ず `None`
- `slackMessageId` が `None` のまま成立・取り消しが起きたときの通知は、スレッド追記ではなく新規投稿として流す。**`None` は「投稿していない」も「3 回試して諦めた」も区別せず表す**——区別のための列は持たない（bot 投稿は通知であって業務の記録ではなく、イベントの真は DB にある）
- 状態遷移は `IS_OPEN → IS_CONFIRMED → IS_FINISHED` が本線。`IS_OPEN → IS_FAILED`（締切）、`IS_OPEN / IS_CONFIRMED → IS_CANCELED`（取り消し）。`IS_FINISHED` からはどこへも動かない

**保存されるデータの例**

| title | startAt | closeAt | minEntries | state | confirmedAt |
|---|---|---|---|---|---|
| 今日ラーメン行きたい | 8/21 12:00 | 8/21 12:00 | 4 | IS_CONFIRMED | 8/21 11:02 |
| 金曜スマブラやろう | 8/22 19:00 | 8/22 18:00 | 6 | IS_OPEN | — |
| 朝ラン誰か | 8/21 07:00 | 8/21 07:00 | 2 | IS_FAILED | — |

### `EventEntry` — 参加

```scala
case class EventEntry(
  id:        Option[Id],                    // 管理 ID（永続化前は None）
  eventId:   Event.Id,                      // どのイベントか
  memberId:  Member.Id,                     // 誰が押したか（成立まで誰にも表示しない）
  updatedAt: LocalDateTime = Now,           // データ更新日
  createdAt: LocalDateTime = Now            // データ作成日
) extends EntityModel[Id]

object EventEntry:

  /** 参加の識別子 */
  type   Id = Id.Repr
  object Id extends Entity.Id[Long]
```

**ここで型が語っていること**
- 状態を持たない。参加は「ある」か「ない」かだけ——取り消しは行を消す（→ [論点2](#論点2参加の取り消しは行を消す)）

**型では守れない決めごと**
- `(eventId, memberId)` は一意。同じイベントに 2 回参加できない
- イベントを立てた瞬間に、立案者の参加が 1 行できる。**立案者の行は参加の取り消しでは消せない**（やめたいときはイベントの取り消し）

**保存されるデータの例**

| eventId | memberId | 意味 |
|---|---|---|
| 1 | 10 | 立案者の 1 行（公開と同時にできる） |
| 1 | 24 | 参加を押した人 |

### `Place` — 店

```scala
case class Place(
  id:            Option[Id],                // 管理 ID（永続化前は None）
  googlePlaceId: Option[String],            // Google の店への参照（手動登録なら None）
  name:          String,                    // 店名（会員が付けた呼び名。登録時に Google から転記して確定）
  updatedAt:     LocalDateTime = Now,       // データ更新日
  createdAt:     LocalDateTime = Now        // データ作成日
) extends EntityModel[Id]

object Place:

  /** 店の識別子 */
  type   Id = Id.Repr
  object Id extends Entity.Id[Long]
```

**ここで型が語っていること**
- `googlePlaceId` が `Option` なのは、API に無い店・社食・オフィス内の場所を手動登録できるから

**型では守れない決めごと**
- `googlePlaceId` は（値があるとき）一意。同じ Google の店から 2 行作らない
- `name` は会員の入力データ（登録時に Google の名前を転記して確定。共有マスタなので誰でも直せる）。住所・営業時間などは保存せず、表示のたびに Google から取得する（→ [論点4](#論点4店名は写しではなく会員が付けた呼び名として持つ)）

**保存されるデータの例**

| googlePlaceId | name |
|---|---|
| ChIJ…abc | 麺屋いちば |
| — | 社食（本社 3F） |

### `PlaceReview` — レビュー

```scala
case class PlaceReview(
  id:        Option[Id],                    // 管理 ID（永続化前は None）
  placeId:   Place.Id,                      // どの店か
  memberId:  Member.Id,                     // 書いた人（実名で表示する）
  star:      Short,                         // 星（1〜5）
  comment:   String,                        // 一言（時間帯・行列もここに自由に書く）
  updatedAt: LocalDateTime = Now,           // データ更新日
  createdAt: LocalDateTime = Now            // データ作成日
) extends EntityModel[Id]

object PlaceReview:

  /** レビューの識別子 */
  type   Id = Id.Repr
  object Id extends Entity.Id[Long]
```

**ここで型が語っていること**
- イベントへの参照を持たない。レビューは店と会員だけの世界で完結する（確定済みの要件）

**型では守れない決めごと**
- `star` は 1〜5（→ [論点3](#論点3成立人数と星を値オブジェクトにしない)）
- 編集・削除できるのは本人だけ。1 人 1 店に複数書ける（一意制約なし）
- 削除は物理削除（行を消す）。訪問日は持たない——`createdAt` が実質の訪問日（→ [論点6](#論点6レビューに足さなかったもの)）

**保存されるデータの例**

| placeId | memberId | star | comment |
|---|---|---|---|
| 5 | 24 | 4 | 11:45 なら並ばず入れた。味玉推し |

## 区分値の考え方

区分値は `Event.Status` の 1 つだけ。**正が「生きている・進んでいる」、負が「途中で終わった」**。

| 値 | code | 意味 | 一覧に出るか |
|---|---|---|---|
| `IS_OPEN` | 1 | 募集中 | 出る（進捗は分数「2/4」） |
| `IS_CONFIRMED` | 2 | 成立。名前が開示されている | 出る（進捗は実数「行く人 5 人」。分数は出さない） |
| `IS_FINISHED` | 3 | 終了（集合日時を過ぎた） | 当日中だけ出る。翌日から参加者の履歴のみ |
| `IS_FAILED` | -1 | 不成立（締切までに届かず） | 出ない。各自の履歴のみ |
| `IS_CANCELED` | -2 | 取り消し | 出ない。成立後の取り消しは参加者の履歴に見える |

業務には「成立前の取り消し」「成立後の取り消し」の 2 語があるが、**区分値は `IS_CANCELED` 1 つ**。どちらだったかは `confirmedAt` の有無で読む（呼称の数と区分値の数は一致しなくてよい）。

## エンティティをまたぐルール

- **成立判定**: `Event` の参加数（`EventEntry` の行数）が `minEntries` に達したら成立。立案者の 1 行を含めて数える。**判定は参加を受け付けた処理の中で即時に行う**（バッチが動かすのは締切→不成立と集合日時→終了、それに期限切れセッションの掃除だけ。判定が重なったら成立を優先）
- **成立は 1 回だけ起きる**: 成立にする更新に「`state` がまだ `IS_OPEN` である」ことを条件として付ける。同時に最後の 2 人が押しても成功するのは 1 つで、成立後に人数が再び達しても 0 行しか更新されない。**Slack への通知はこの更新が 1 行成功したときだけ出す**（`confirmedAt` の二重書きと二重投稿を同じ 1 手で防ぐ）
- **参加できるかは状態と現在時刻の両方で決まる**: `(IS_OPEN かつ 現在 < closeAt)` または `(IS_CONFIRMED かつ 現在 < startAt)`。状態だけを見ると、バッチが動く前の数十秒に締切後の参加を受け付けて成立させてしまう。**その時刻ちょうどは締まっている**——バッチは `closeAt ≦ 現在` を `IS_FAILED` に、`startAt ≦ 現在` を `IS_FINISHED` にするので、隙間も重複も出ない
- 参加の取り消し（行の削除）も同じ 2 状態に限る。`IS_FAILED`・`IS_CANCELED`・`IS_FINISHED` の参加行は変更しない。**立案者が自分の参加だけを取り消せるのは `IS_CONFIRMED` のときだけ**（`IS_OPEN` で消せると立案者不在のイベントが募集を続ける。成立後はその理由が効かず、副産物として「立案者は必ず成立名簿に載る」がなくなり、名簿を何日も重ねて立案者を絞る観測が成立しなくなる）
- **開示する名簿は表示名の五十音順**（`Member.nameKana` で並べる）。参加順・作成時刻・ID と相関する並びにしない（先頭＝立案者の特定を防ぐ）。名簿は都度計算＝**いま行く人**なので、開示後に取り消した人は名簿から消える（それが見えることは要件で許容した）
- **進捗（2/4）・本人の履歴・店の平均点・提案 3 件は保存しない**。すべて都度計算する。進捗は募集中だけ分数で出し、成立後は実数だけを出す
- **提案の並び順**: `(3 × 全店の平均星 + その店の星の合計) ÷ (3 + その店のレビュー件数)` の降順（縮小平均）。同点はレビュー件数の多い順、それも同じならレビューが新しい順。3 件に足りない分は「登録済みでレビューがまだ無い店」→ Google（会社周辺・評価順）の順に埋める

## 入力値の範囲

DDL を書くときに必ず要る値。ここに 1 表で置く。

| 対象 | 範囲・規則 | なぜそう決めたか |
|---|---|---|
| `Event.title` | 1〜60 文字・改行不可 | 場所をタイトルに書くと決めたので実質の「場所欄」になる。1 行で読める長さに収める |
| `Event.startAt` | 現在より後、1 年先まで | 上限が無いと誤入力の「2099 年」が消えずに残る |
| `Event.closeAt` | 現在 < `closeAt` ≦ `startAt`。既定は `startAt` と同じ | 下限が無いと参加できないイベントが作れる |
| `Event.minEntries` | 2〜50。既定は 4 | `Short` のままだと 32767 まで通る。既定は要求文の「4 人集まったら行く」に合わせる |
| `Event.code` | `SecureRandom` の 128bit 相当（URL-safe Base64 で 22 文字）・**一意**・衝突したら再生成 | 推測されると募集中イベントを総当たりで列挙できる |
| `Member.uuid` | UUIDv4 | 連番を外に晒さないため |
| `Member.email` | 一意 | ログイン ID |
| `Member.name` | 1〜20 文字。**一意にしない・変更できる** | 同姓同名がありうる。変更すると過去の名簿とレビューの表示も追従する（名前を保存していないため） |
| `Member.nameKana` | 1〜40 文字・**全角カタカナのみ**・必須 | ひらがな混在は文字コード順で二群に割れて名簿の並びが壊れる |
| `PlaceReview.star` | 1〜5 | 星は 5 段階 |
| `PlaceReview.comment` | 0〜200 文字・改行不可・**任意**（星だけでも出せる） | 「一言」なので構造化も長文もしない |
| `Place.name` | 1〜40 文字。Google の店を指していない行は名前を**一意** | 「社食」「3F社食」のような表記揺れは止まらない、と割り切る |
| すべての日時 | **JST 固定**で保存・比較。日付の境界は JST の 00:00 | JVM・DB・コンテナで基準が違うと、同じイベントが端末によって出たり消えたりする |

実装メモ: 現在時刻は注入可能な `Clock` から取る（テストで固定できるようにする）。

## 制約をどこで守るか

「型では守れない決めごと」を**どの層で守るか**を決める。ここが決まっていないと、`apply` に検証を入れて Slick の読み出しで例外が飛ぶ、あるいは同じ検証がリクエスト型とモデルの 2 か所に散る。

| 層 | 守るもの |
|---|---|
| DB | 一意（`member.email` / `member_session.token` / `event.code` / `event_entry (event_id, member_id)` / `member_password.member_id` / Google の店を指していない `place.name`）、CHECK（`min_entries` は 2〜50、`star` は 1〜5、`close_at ≦ start_at`） |
| モデルを作るとき | `state` と `confirmedAt` の連動。**`apply` ではなく生成用のメソッドで守る**（`apply` に入れると DB からの読み出しでも走って例外になる） |
| サービス層 | 参加できる状態と時刻の判定、立案者の参加行を消せる条件、成立を 1 回だけにする条件付き更新、店名の上書きの突き合わせ（409） |
| リクエスト検証 | 文字数・文字種・必須（`title`・`nameKana`・`comment` など入力の形）。**同じ検証をモデル側にも書かない**——入口で弾く |

## API に載せるフィールド

匿名は「表示しないだけ」で守ると決めたので、**何を載せてよいかを書いておかないと「立案者 ID が混ざっていない」ことを検証できない**。レスポンスのキー集合をこの表と突き合わせるテストを書く。

| レスポンス | 載せるもの | 絶対に載せないもの |
|---|---|---|
| イベント一覧 | `code`・タイトル・集合日時・締切・成立人数・参加数・状態・本人向けの印 | `Event.memberId`、`EventEntry.memberId`、`Member.id` |
| イベント詳細（募集中） | 上に同じ | 同じ（成立前は会員を指す値を一切載せない） |
| イベント詳細（成立後） | 上＋名簿（`Member.uuid`・表示名） | `Event.memberId`、`Member.id`、`EventEntry.id`、参加した時刻 |
| イベント詳細（最小表示） | 「このイベントは終了しました」の一文だけ | 状態・タイトル・人数・名前のすべて |
| 履歴（本人） | 自分が立てた・押したイベントと自分のレビュー | 他の会員を指す値 |
| レビュー一覧 | `Member.uuid`・表示名・星・一言・投稿日時 | `Member.id`、`Member.email` |

名簿で会員を指すのは `Member.uuid` だけ（`id` は DB の中だけ）。参加した時刻を載せない理由は、並び順を五十音にした理由と同じ。

## 置き場所（コンテキスト）

**判断:** `member`（Member / MemberPassword / MemberSession）、`event`（Event / EventEntry）、`place`（Place / PlaceReview）。`common` は作らない。

**理由:** 変更の理由・ライフサイクル・公開性が 3 つとも違う（詳細は [02_analysis.md](./02_analysis.md)）。common に入れてよい 3 条件を満たすデータが無い。

---

# 付録A：今回やらないこと

- 定員（上限人数）。あと乗り自由と整合させる
- 公開後のイベントの編集（取り消して立て直す）
- グループ分け・マルチテナント（1 社・全員に見える）
- 登録制限（メールドメイン制限・招待制）。根拠は「URL が出るのは社内 Slack だけで、混入が起きたら DB 直接対応で止める」——**「URL を知っている人しか来ない」は根拠にしない**（閲覧を URL で開いているので、社外の第三者が会員になり匿名でイベントを立てて bot に社内へ投稿させられる）。メールドメインの前方一致チェック 1 箇所で塞げるので、費用対効果はここに残す
- 通報・強制削除・管理者ロール・管理画面（設定は設定ファイル）
- 参加者からの手動の店提案（提案はアプリの自動のみ）
- 立案時に店を選ぶ機能（場所を伝えたいときはタイトルに書く → 論点5）
- レビューとイベントの紐づけ
- 会員個人の位置情報の利用
- 集計・レポート機能
- 参加の取り消し履歴（取り消したという記録は残さない → 論点2）
- レビューへの写真の添付（要求は「星と一言」。画像ストレージ一式が増えるため作らない）
- 重複した店を 1 つにまとめる操作（手動登録の前に前方一致で「これですか？」を出すところまでで止める）
- 行き先の記録（成立後に決まった店をイベントに残さない。判断が変わる条件＝当日に集合場所が分からず迷った事例が出たとき）
- 個人向けのプッシュ通知（一覧の本人向けの印と履歴の入口で足りる。判断が変わる条件＝Slack を見ずに成立に気づかなかった事例が出たとき）
- bot 投稿の再送（3 回試して諦める。投稿状態の列もバッチも持たない）
- 個人を持たない閲覧数の計測（「不参加の記録は存在しない」原則とは衝突しないが、今回は持たない → 論点13）
- アプリ名・アイコン・ホーム画面からの起動先の決定（入口の制作物は実装段階で決める）

# 付録B：判断の記録

### 論点1：取り消しを 1 つの状態で表し、成立前後の違いは `confirmedAt` で読む

**判断:** `IS_CANCELED` は 1 つ。成立していたかは `confirmedAt`（成立日時）の有無で判定する。

見え方は「成立前の取り消し＝本人の履歴のみ／成立後の取り消し＝参加者にも見え、Slack にも流れる」と違うので、区別は必要になる。`IS_CANCELED_BEFORE` / `IS_CANCELED_AFTER` の 2 値に割る案もあったが、成立の事実はすでに `confirmedAt` として持っており、状態を 2 つにすると同じ事実を 2 か所に書くことになる。`confirmedAt` は成立時刻の記録・Slack への追記にも使うので、増えるものが無い。

**代償:** `state` だけを見ても成立後の取り消しかは分からず、必ず `confirmedAt` とセットで読む。この連動は「型では守れない決めごと」に明記した。

**判断が変わる条件:** 取り消し理由や取り消し時刻を独立に扱う要求が来たら、取り消しを別モデルに切り出す方が良くなる。

### 論点2：参加の取り消しは行を消す

**判断:** `EventEntry` に状態を持たせず、取り消しは物理削除。`(eventId, memberId)` の一意制約で再参加は再 INSERT になる。

「押したものが本人の履歴に残る」という要件は、**いま押している参加＋終了・不成立時点で残っていた参加**で満たせる。取り消した事実まで残すと「誰が断ったか」に近い記録が生まれ、「不参加という記録自体が存在しない」という確定済みの原則と衝突する。消えて困る人がいない（教材の「消していいのは、消えても誰も困らないデータだけ」を満たす、数少ないケース）。

**代償:** 「一度参加して取り消した」ことは本人にも分からなくなる。

**復旧との関係:** 物理削除なので、バックアップから過去時点へ戻すと**取り消した人の参加行が復活する**（成立していれば開示名簿にその人の名前が出る）。「消えて困る人がいない」は通常運転の話で、復旧という操作では成り立たない——取り消した参加は**復旧でも復元されないべきデータ**であり、復旧手順に是正を含める。

**判断が変わる条件:** ドタキャン分析のような要求が来たとき。ただしそれは匿名の原則ごと見直す話になる。

### 論点3：成立人数と星を値オブジェクトにしない

**判断:** `minEntries: Short`（2 以上）と `star: Short`（1〜5）は素の型で持ち、検証はモデルを作るときに行う。

どちらも使う場所が 1 か所で、型を作っても守れる範囲が広がらない（教材の `Money` / `Quantity` はコンテキスト横断で頻出だから型にした）。「増やしすぎない」に倒す。

**判断が変わる条件:** 星の集計・人数の比較ロジックが複数箇所に散り始めたら型に昇格する。

### 論点4：店名は「写し」ではなく「会員が付けた呼び名」として持つ

**判断:** `Place` に保存するのは `googlePlaceId`（規約上、無期限に保存してよい唯一のデータ）と `name` だけ。そして `name` は Google コンテンツの写しではなく、**会員の入力データ**として持つ——登録時に Google の検索結果を選ぶと店名がフォームに転記され、会員がそのまま（または編集して）確定する。共有マスタなので、あとから誰でも直せる。住所・営業時間・写真・Google の評価は保存せず、表示のたびに取得する。

place_id だけで運用できないかも検討したが、成立しない。①レビュー一覧を開くたびに店の数だけ Google を呼ぶことになる ②Google 障害時に店名すら出ず、デグレード方針（登録済みの店だけで出す）が成立しない ③**閉店すると place_id は無効になることがあり、「レビューは無期限に残す」という前提を Google のデータでは守れない**——5 年後のレビューが名無しになる。表示に要る最低限（名前）は自前で持つしかない。

「写し」でなく「会員の入力」とする理由: 規約上の性格が「Google コンテンツのキャッシュ（30 日期限）」から「place_id を参照するユーザー生成データ」に変わる。30 日ごとに名前をリフレッシュする案は、バッチと課金が増えるだけで閉店時の名無し問題を解決しないので採らない。

**代償:** Google 上の正式名と社内の呼び名がズレうる（ただし「裏のラーメン屋」のような社内の通り名を残せるのは、このアプリではむしろ利点）。この規約整理は一般的な実務慣行であって Google の保証ではない。

**判断が変わる条件:** 公開サービス化するとき（法務レビュー必須）。または Google がユーザー確定入力の扱いを明文化したとき。

### 論点5：イベントは場所を持たない

**判断:** `Event` から店への参照（placeId）も、場所の自由記述も持たない。場所を伝えたいときはタイトルに書く（「今日ランチ@サイゼ」）。

要求文には「店から場所を選んでもいい」とあったが、選ばないことのほうが多いと明記されており、店の「決定」も記録しないと確定済み。参照を持つと event → place の依存が 1 本増え、自由記述を持つと思いつきの雑な記入が溜まりやすい。イベントは「いつ・何人で」だけを持ち、店の世界（店リスト・レビュー・提案）とは完全に独立させる。

**代償:** 立案時に登録済みの店を選んでレビューへ飛ぶ、という導線は作れない。

**判断が変わる条件:** 「イベントから店のレビューを開きたい」という要望が実際に出たとき。そのときは placeId（参照）を足す——自由記述ではなく。

### 論点6：レビューに足さなかったもの

**判断:** `PlaceReview` は placeId・memberId・star・comment（＋updatedAt/createdAt）だけで確定。候補に挙がった 4 つを検討して、すべて落とした。

- **訪問日（visitedAt）**: 「食べたらそのまま書ける」が本線なので投稿日≒訪問日。後日投稿のズレは一言に書けば足りる。任意入力にしても大半が空欄になり、入力項目が増える分だけ軽さが削れる
- **編集済みフラグ**: `updatedAt ≠ createdAt` で計算できる
- **店名のスナップショット**: 店は消さない共有マスタで参照が切れない。呼び名が変われば過去レビューも新しい呼び名で表示されてよい（同じ店なので）
- **論理削除（deletedAt）**: 本人の意思で消すものは「消えて困る人がいない」（参加の取り消しと同じ整理）。論理削除にすると全クエリに除外条件が要り、書き忘れがバグになる。復元要求もない

**判断が変わる条件:** 「あとから思い出して書く」使い方が実際に多いと分かったら訪問日を足す。復元・監査の要求が出たら論理削除に切り替える。

### 論点7：Member.Status を残す

**判断:** 会員を無効にする操作・画面は今回作らないが、`Member.Status`（IS_ACTIVE / IS_INACTIVE）は持つ。

要件に退会・無効化は無い（レビューで「根拠のない区分値」と指摘された）。それでも残すのは、「不適切な事態が起きたら DB 直接対応」という確定済みの運用の**受け皿**が必要だから——state を 1 行変えるだけでログインを止められる。無効会員の既存の参加・レビューはそのまま残る（消えて困る人がいないものだけ消す原則）。

**代償:** 使われないかもしれない区分値を 1 つ持つ。

**判断が変わる条件:** 退会機能の要求が出たら、そのとき遷移・画面ごと設計する。

### 論点8：成立人数の下限は 2 のまま（立案者が確定することを許容する）

**判断:** 下限は 2 人（立案者込み）。2 人で成立した場合、名簿のもう 1 人から見れば立案者は確定するが、許容する。

立案者は自分も参加するので**成立名簿には必ず立案者が含まれる**——特定の確率は元から 1/n で、n=2 はその端にあたるだけで設計の連続線上にある。そして匿名が本当に守っているのは**不成立の局面**で、そこでは一覧からも消え、URL を踏んでも一文しか出ず、Slack にも何も流れない。「誘いが流れたときに誰も傷つかない」は保たれる。成立とは「相手が行くと言った」状態なので、そこで誰の誘いかが分かるのは害よりお礼が言える方向に働く。下限を 3 に上げると「一人来てくれれば行くランチ」が作れなくなり、要求の中心にある軽さを失う。

**代償:** 2 人イベントでは相手から立案者が確定する。5 役のレビューが独立に指摘した唯一の項目でもある。

**判断が変わる条件:** 2 人イベントで「誰が誘ったか」を隠したい要望が実際に出たとき。そのときは下限を上げるのではなく、2 人成立時だけ開示を遅らせるなどの別案を検討する。

### 論点9：開示後の名簿からの離脱は見えるままにする

**判断:** 名簿は都度計算（＝いま行く人）のまま。成立後に取り消すと公開済みの名簿から名前が消え、それが他の参加者に見えることを要件として明記し、参加を押す前に伝える。

「不参加の記録は存在しない」を開示後にも通すには、成立時点の名簿を固定して保存する（スナップショット）しかない。だが「消さない設計にしたのでスナップショットは不要」という判断を崩す上に、**名簿にいるのに来ない人**が生まれて集合の役に立たなくなる。名簿の役割は「いま誰と行くか」なので、現在の参加を映すほうを採る。

**代償:** 「入って抜けた」が参加者に見える。押す前に伝えることで、驚きにはしない。

**判断が変わる条件:** 離脱が理由で押すのをためらう、という声が実際に出たとき。

### 論点10：匿名が守られる範囲を「表示境界とログ」と定義する

**判断:** 匿名は表示ルールで守ると決めたので、守られる範囲を明示する——**アプリのレスポンスとログまで**。会員識別子とセッショントークンはログに出さない（出すならハッシュ化した相関 ID）、リクエストボディはログしない、例外通知に会員 ID を含めない。そして **DB 権限を持つ人に対して匿名は成立しない**ことを限界として書く。

範囲を書かないと「立案者 ID が混ざっていない」ことを何と比べて判定するのかが決まらない。レスポンス側は「API に載せるフィールド」の表が正となり、キー集合の突き合わせで検証できる。ログ側は出さないものを列挙して守る。

**代償:** 障害調査で会員を特定しにくくなる（相関 ID をたどる手間が増える）。

**判断が変わる条件:** 監査要件が生まれたとき。そのときは「誰が何を見たか」を残す設計に変わり、この定義ごと見直す。

### 論点11：`place.name` は DB 直接対応でも復旧できない唯一のデータ

**判断:** 店名は共有マスタなので誰でも直せる。更新時に手元の更新日時を突き合わせて他の人が先に直していたら弾く（409）が、**変更前の値は残さない**。

変更履歴を持てば復旧できるが、履歴テーブルが 1 つ増える。頻度が低く、間違えても次の人が直せる種類のデータなので持たない。ただし「問題が起きたら DB 直接対応」という運用は**元の値がどこかに残っていること**を前提にしているので、そこから外れる唯一のデータであることを明記する。

**代償:** 誰かが上書きした元の呼び名は取り戻せない（全レビューの見出しが一度に変わる）。

**判断が変わる条件:** 悪意のある書き換えが実際に起きたとき。そのときは変更履歴か、更新できる人の制限を足す。

### 論点12：開示するのは表示名だけ（部署・顔写真は持たない）

**判断:** 成立時に開示するのは表示名（と並べるための読み）だけ。部署・役職・顔写真・Slack のアカウントは持たない。

要求は「誰が行くかがみんなに見える」で、職場の名前が分かれば足りる。属性を足すほど「誰と行くか」の判断材料が増え、参加が選別に近づく。名前だけなら「知っている人か」の判断は各自の記憶に委ねられる。

**代償:** 同姓同名が名簿に並ぶと区別できない（表示名を一意にしないので起こりうる）。入って間もない人は名前を見ても誰か分からない。

**判断が変わる条件:** 名前だけでは誰か分からず参加をためらう、という声が実際に出たとき。

### 論点13：閲覧の計測をしない

**判断:** 詳細を何人が開いたかを数えない（個人を持たないカウンタも持たない）。

価値仮説の後半「断りやすい」は本来、閲覧に対する参加率でしか観測できない。個人を持たないカウンタなら「不参加の記録は存在しない」原則とは衝突しないが、数字が出れば「開いたのに押していない人が何人いる」という圧の材料になりうる。要件の中心は記録を残さないことなので、そちらに倒す。

**代償:** 後から足してもリリース初期のデータは永久に欠ける。断りやすさは数字では確かめられず、聞き取りで判断することになる。

**判断が変わる条件:** 「使われていないのはなぜか」を数字で説明する必要が出たとき。

### 論点14：社内公開への昇格条件

**判断:** これは研修課題として作る。社内に公開してよいのは、①費用の上限と止める人が決まっている ②利用者向けの説明（画面の 2 行）が入っている ③オーナー部署と苦情の一次受けが決まっている の 3 つが揃ったときとする。

論点4 に「公開サービス化するとき（法務レビュー必須）」はあるが、社内公開のゲートが無かった。社内公開でも苦情は人に来て、課金は発生する。

**代償:** 3 つが揃うまで社内には出せない（作っても使われない期間ができる）。

**判断が変わる条件:** 特になし。3 つはいずれも実装ではなく合意で満たせる。

---

# 付録C：画面と遷移

図は作らず、画面の一覧で持つ。SvelteKit のルートと、どの画面がどのエンティティを読むかがこれで決まる。

| 画面 | ログイン | 表示するもの | 遷移元 → 先 |
|---|---|---|---|
| イベント一覧 | 不要 | Event（集合日時の昇順・終了当日は末尾）＋参加数。本人の印はログイン時のみ | 入口（Slack の URL・ホーム画面）→ 詳細・作成・店リスト・履歴 |
| イベント詳細（募集中） | 不要 | Event＋参加数（分数） | 一覧・Slack の URL → 参加（ログイン）・取り消し |
| イベント詳細（成立） | 不要 | Event＋名簿（表示名・五十音順）＋実数＋提案 3 件 | 同じ → 参加・取り消し・店詳細 |
| イベント詳細（終了当日） | 不要 | 成立と同じ＋「終了」ラベル | 一覧 → 店詳細 |
| 最小表示 | 不要 | 「このイベントは終了しました」＋履歴への 1 行 | URL 直踏み → ログイン |
| イベント作成 | 必須 | 入力（前回の値を初期値）→ **確認** → 公開 | 一覧 → 詳細 |
| 履歴 | 必須 | 自分が立てた・押したイベント／自分のレビュー（1 タブ） | 一覧 → 詳細・レビュー編集 |
| 店リスト | 不要 | 会社周辺の Google 検索結果＋登録済みの店とレビュー | 一覧・イベント詳細 → 店詳細 |
| 店詳細 | 不要 | Place＋PlaceReview 一覧（実名） | 店リスト → レビュー投稿・店名の編集 |
| レビュー投稿・編集 | 必須 | 星と一言。同じ店に直近の自分のレビューがあれば「前回のを直す／新しく書く」を聞く | 店詳細・履歴 → 店詳細 |
| 店の手動登録 | 必須 | 名前の入力。前に既存の店を前方一致で出して「これですか？」 | レビュー投稿 → レビュー投稿 |
| ログイン | — | メール＋パスワード | 参加・作成・レビュー投稿 → 元の画面 |
| 会員登録 | — | メール・パスワード・表示名・**表示名の読み** | ログイン → 一覧（初回は匿名の範囲の説明を 1 回出す） |
