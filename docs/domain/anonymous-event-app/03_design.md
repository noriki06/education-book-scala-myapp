# 匿名イベントアプリ — 詳細要件定義

対象は [01_requirements.md](./01_requirements.md) で確定した全要件。コンテキストは会員 `member`／イベント `event`／店 `place` の 3 つ（[02_analysis.md](./02_analysis.md)）。追加するエンティティは次の 7 つ。

| エンティティ | テーブル | コンテキスト | 役割 | 主な状態 |
|---|---|---|---|---|
| `Member` | `member` | member | 会員（ログインする本人） | 有効 / 無効 |
| `MemberPassword` | `member_password` | member | 認証情報 | — |
| `MemberSession` | `member_session` | member | ログインセッション | — |
| `Event` | `event` | event | イベント。匿名で立ち、成立・終了までを持つ | 募集中 / 成立 / 終了 / 不成立 / 取り消し |
| `EventEntry` | `event_entry` | event | 参加。誰がどのイベントに押したか | — |
| `Place` | `place` | place | 店。Google の店への参照＋手動登録 | — |
| `PlaceReview` | `place_review` | place | レビュー。星と一言・実名 | — |

実装メモ: 認証まわり（Member / MemberPassword / MemberSession）は、雛形の認証コード（Customer 系）をリネームして流用する（[naming.md](./naming.md)）。設計上の扱いは他と同じく、このアプリ自身のエンティティである。

## 用語（ユビキタス言語）

サービス全体で使う呼称をここで 1 つに決める。会議でも仕様書でもコードでも、この呼称だけを使い、言い換えない。

| 呼称（これで統一する） | 何を指すか | 言い換えない |
|---|---|---|
| イベント | 匿名で立ち、成立・終了までを含む 1 件 | 「募集」「企画」 |
| 立案者 | イベントを立てた本人（ずっと匿名） | 「主催者」「ホスト」「オーナー」 |
| 参加 | 会員がイベントに押した 1 件 | 「エントリー」「申込」 |
| 参加者 | 参加を押している会員（立案者を含む） | 「メンバー」「出席者」 |
| 会員 | ログインする本人 | 「社員」「アカウント」「ユーザー」 |
| 集合日時 | 実際に集まる日時。過ぎたら終了 | 「開催日時」「開始時刻」「集合日時」 |
| 締切 | **成立判定を打ち切る期限**（既定＝集合日時）。成立後の参加受付は集合日時まで続くので「参加を受け付ける期限」ではない | 「応募期限」「エントリー期限」 |
| 成立人数 | 成立に必要な人数（立案者込み・2 以上） | 「定員」（上限の意味になる。定員は作らない） |
| 募集中 | 立ってから、成立か締切までの状態 | 「受付中」「オープン」 |
| 成立 | 参加が成立人数に達し、名前が開示された状態 | 「開催決定」「マッチング」 |
| 開示 | 成立した瞬間に、参加者の名前が全員に見えるようになること | 「公開」「発表」 |
| 終了 | 集合日時を過ぎた状態（当日中は一覧に残る） | 「開催済み」「クローズ」 |
| 不成立 | 締切までに人数が届かず終わったこと | 「流れた」「キャンセル」 |
| 取り消し | 立案者がイベントをやめること | 「取り下げ」「中止」 |
| 店 | レビューと提案の紐づけ先（社食・オフィス内の場所も含む） | 「お店」「店舗」「スポット」 |
| 提案 | 成立時にアプリが自動で出す店（3 件程度） | 「おすすめ」「レコメンド」 |
| レビュー | 星（5段階）と一言 | 「口コミ」「評価」 |
| 履歴 | 自分が立てた・押したイベントと自分のレビューの一覧（本人だけが見る） | 「アクティビティ」「マイページ」 |
| 表示名の読み | 表示名をカナで書いたもの（名簿を五十音順に並べるために持つ） | 「フリガナ」「ヨミガナ」 |

## ER 図

まず、データの繋がりだけを確認します。何が必須か・日時をどう持つかは、この図では決めません。

![匿名イベントアプリの ER 図。左に会員（Member）があり、会員 ID・公開用 UUID・メール・表示名とその読み（名簿の並び順に使う）・状態を持つ。その下に認証情報（MemberPassword）が 1 対 1、セッション（MemberSession）が 1 対多で繋がる。中央の参加（EventEntry）はイベント ID と会員 ID（成立まで誰にも見えない）を持ち、イベント × 会員の組で一意。会員とは 1 対多、右のイベント（Event）とは多対 1。イベントは公開用 ID・立案者の会員 ID（誰にも表示しない・点線の参照）・内容・集合日時・締切・成立人数・投稿先チャンネル・投稿の記録・成立日時・状態を持ち、店への参照は持たない。右下の店（Place）は Google の店への参照（手動登録なら無し）と店名（会員が付けた呼び名・誰でも直せる）を持つ。中央下のレビュー（PlaceReview）は店 ID・会員 ID（実名で出る）・星・一言を持ち、会員と店それぞれに 1 対多で繋がる。注記: この図で確認するのは「どこに何があって、いくつ繋がるか」だけ。イベントは店を知らず、place と member の世界もイベントを知らない。updatedAt / createdAt と各エンティティの管理 ID は省略](./img/er.svg)

- コンテキストをまたぐ参照は **ID だけ・一方向**（event → member、place → member）。**イベントは店を知らず**、place と member の世界もイベントを知らない
- `EventEntry` は会員とイベントの交差。**(eventId, memberId) の組で一意**

## EntityModel

形だけを書き、メソッドや検証は書かない。型では表せない決めごとは文章で添える。

### `Member` — 会員

```scala
case class Member(
  id:        Option[Id],                    // 管理 ID（永続化前は None）
  uuid:      UUID,                          // 公開用 UUID（連番を外に晒さない）
  email:     String,                        // ログイン ID（一意）
  name:      String,                        // 表示名（開示・レビューに出る）
  nameKana:  String,                        // 表示名の読み（名簿の並び順に使う）
  state:     Status        = Status.IS_ACTIVE, // アカウント状態
  updatedAt: LocalDateTime = Now,           // データ更新日
  createdAt: LocalDateTime = Now            // データ作成日
) extends EntityModel[Id]

object Member:

  /** 会員の識別子と状態 */
  type   Id = Id.Repr
  object Id extends Entity.Id[Long]

  type   UUID = UUID.Repr
  object UUID extends Entity.Id[String]

  /** アカウント状態 */
  enum Status(val code: Short) extends EnumStatus[Short]:
    case IS_INACTIVE extends Status(code = -1) // 無効
    case IS_ACTIVE   extends Status(code =  1) // 有効
```

**ここで型が語っていること**
- `uuid` が公開用、`id` は DB の中だけ。開示・レビュー表示の API レスポンスで会員を指す識別子として `uuid` を使い、連番を外に晒さない

**型では守れない決めごと**
- `email` は一意
- `name` は職場で誰だとわかる名前。**一意にしない**（同姓同名がありうる）。**変更できる**——名前を保存していないので、変更すると過去の名簿とレビューの表示も追従する
- `nameKana` は必須・全角カタカナのみ。ひらがなとの混在を許すと文字コード順で二群に割れて並びが壊れる。**正しい読みが入っていることは保証できない**（並びが読み順になるのは全員が正しく入れた場合だけ）
- 無効化（`IS_INACTIVE`）の操作・画面は作らない。問題発生時の DB 直接対応用の受け皿（→ [論点7](#論点7member-status-を残す)）

**保存されるデータの例**

| email | name | nameKana | state |
|---|---|---|---|
| ohira@example.co.jp | 大平 | オオヒラ | IS_ACTIVE |

### `MemberPassword` — 認証情報

```scala
case class MemberPassword(
  id:        Option[Id],                    // 管理 ID（永続化前は None）
  memberId:  Member.Id,                     // 会員 ID（1 会員に 1 行）
  hash:      String,                        // パスワードハッシュ
  updatedAt: LocalDateTime = Now,           // データ更新日
  createdAt: LocalDateTime = Now            // データ作成日
) extends EntityModel[Id]

object MemberPassword:

  /** 認証情報の識別子 */
  type   Id = Id.Repr
  object Id extends Entity.Id[Long]
```

**ここで型が語っていること**
- 他のエンティティと同じく自分の管理 `Id` を持つ（雛形の CustomerPassword と同じ形。ハッシュ化・照合の処理もコンパニオンが担う）

**型では守れない決めごと**
- `memberId` は一意——**1 会員に 1 行**
- 保存するのはハッシュだけ（平文・可逆な形は持たない）

**保存されるデータの例**

| memberId | hash |
|---|---|
| 10 | $pbkdf2$…（ハッシュ文字列） |

### `MemberSession` — セッション

```scala
case class MemberSession(
  id:        Option[Id],                    // 管理 ID（永続化前は None）
  memberId:  Member.Id,                     // 会員 ID
  token:     Token,                         // セッショントークン（一意・未署名。ixias の Token 型）
  expiresAt: LocalDateTime = Now.plusDays(30), // 有効期限
  updatedAt: LocalDateTime = Now,           // データ更新日
  createdAt: LocalDateTime = Now            // データ作成日
) extends EntityModel[Id]

object MemberSession:

  /** セッションの識別子 */
  type   Id = Id.Repr
  object Id extends Entity.Id[Long]
```

**ここで型が語っていること**
- 自分の `Id` を持つ＝会員と **1:***。スマホと PC で同時にログインできる

**型では守れない決めごと**
- `token` は一意。ログアウトは行の削除（即時失効）。`expiresAt` を過ぎたトークンは無効
- セッションに状態は持たない。ログアウトが行の削除なので「有効なセッション＝行が在る」の一義で足りる

**保存されるデータの例**

| memberId | token | expiresAt |
|---|---|---|
| 10 | a1b2…（ランダム文字列） | 9/23 12:00 |

### `Event` — イベント

```scala
case class Event(
  id:             Option[Id],                      // 管理 ID（永続化前は None）
  code:           Code,                            // 公開用 ID（URL に使う。一意・推測できない）
  memberId:       Member.Id,                       // 立案者（誰にも表示しない）
  title:          String,                          // 内容（「今日ランチ行きたい」）
  startAt:        LocalDateTime,                   // 集合日時（イベントの始まり。過ぎたら終了）
  closeAt:        LocalDateTime,                   // 締切（既定＝集合日時。現在 < closeAt ≦ startAt）
  minEntries:     Short,                           // 成立人数（立案者込み・2〜50。達したら成立）
  slackChannelId: String,                          // 投稿先チャンネル（立案時に選ぶ）
  slackMessageId: Option[String],                  // bot 投稿の記録（投稿前は None）
  confirmedAt:    Option[LocalDateTime],           // 成立した日時（成立前は None）
  state:          Status        = Status.IS_OPEN,  // イベントの状態
  updatedAt:      LocalDateTime = Now,             // データ更新日
  createdAt:      LocalDateTime = Now              // データ作成日
) extends EntityModel[Id]

object Event:

  /** イベントの識別子 */
  type   Id = Id.Repr
  object Id extends Entity.Id[Long]

  /** 公開用 ID（URL 用） */
  type   Code = Code.Repr
  object Code extends Entity.Id[String]

  /** イベントの状態 */
  enum Status(val code: Short) extends EnumStatus[Short]:
    case IS_CANCELED extends Status(code = -2) // 取り消し: 立案者がやめた
    case IS_FAILED   extends Status(code = -1) // 不成立: 締切までに人数が届かなかった
    case IS_OPEN     extends Status(code =  1) // 募集中
    case IS_CONFIRMED extends Status(code = 2) // 成立: 名前が開示されている
    case IS_FINISHED extends Status(code =  3) // 終了: 集合日時を過ぎた
```

**ここで型が語っていること**
- `slackMessageId` が `Option` なのは、**まだ投稿していない状態が存在する**から（教材の `Payment.transactionId` と同型）
- `confirmedAt` が `Option` なのは、成立しないまま終わるイベントがあるから。**成立後に取り消されたかどうか**は `state = IS_CANCELED` かつ `confirmedAt` が `Some` で読む（→ [論点1](#論点1取り消しは-1-つの状態で表す)）
- 立案者の匿名はデータでは守らない。`memberId` は普通に持ち、**表示しない**だけ（アクセス境界の話）

**型では守れない決めごと**
- `code` は**一意**、かつ推測できない値。ログイン不要で詳細が見える唯一の入場鍵で、Slack に URL が流れ続けるので後から変えられない
- `現在 < closeAt ≦ startAt`。過去の `startAt` では作れない（下限が無いと「昨日締切・明日集合」の参加できないイベントが作れる）
- `minEntries` は 2 以上 50 以下
- `state` と `confirmedAt` は連動する: `IS_CONFIRMED`・`IS_FINISHED` なら必ず `Some`、`IS_OPEN`・`IS_FAILED` なら必ず `None`
- `slackMessageId` が `None` のまま成立・取り消しが起きたときの通知は、スレッド追記ではなく新規投稿として流す。`None` は「まだ投稿していない」と「諦めた」を区別しない（区別のための列は持たない）
- 状態遷移は `IS_OPEN → IS_CONFIRMED → IS_FINISHED` が本線。`IS_OPEN → IS_FAILED`（締切）、`IS_OPEN / IS_CONFIRMED → IS_CANCELED`（取り消し）。`IS_FINISHED` からはどこへも動かない

**保存されるデータの例**

| title | startAt | closeAt | minEntries | state | confirmedAt |
|---|---|---|---|---|---|
| 今日ラーメン行きたい | 8/21 12:00 | 8/21 12:00 | 4 | IS_CONFIRMED | 8/21 11:02 |
| 金曜スマブラやろう | 8/22 19:00 | 8/22 18:00 | 6 | IS_OPEN | — |
| 朝ラン誰か | 8/21 07:00 | 8/21 07:00 | 2 | IS_FAILED | — |

### `EventEntry` — 参加

```scala
case class EventEntry(
  id:        Option[Id],                    // 管理 ID（永続化前は None）
  eventId:   Event.Id,                      // どのイベントか
  memberId:  Member.Id,                     // 誰が押したか（成立まで誰にも表示しない）
  updatedAt: LocalDateTime = Now,           // データ更新日
  createdAt: LocalDateTime = Now            // データ作成日
) extends EntityModel[Id]

object EventEntry:

  /** 参加の識別子 */
  type   Id = Id.Repr
  object Id extends Entity.Id[Long]
```

**ここで型が語っていること**
- 状態を持たない。参加は「ある」か「ない」かだけ——取り消しは行を消す（→ [論点2](#論点2参加の取り消しは行を消す)）

**型では守れない決めごと**
- `(eventId, memberId)` は一意。同じイベントに 2 回参加できない
- イベントを立てた瞬間に、立案者の参加が 1 行できる。**立案者の行は参加の取り消しでは消せない**（やめたいときはイベントの取り消し）

**保存されるデータの例**

| eventId | memberId | 意味 |
|---|---|---|
| 1 | 10 | 立案者の 1 行（公開と同時にできる） |
| 1 | 24 | 参加を押した人 |

### `Place` — 店

```scala
case class Place(
  id:            Option[Id],                // 管理 ID（永続化前は None）
  uuid:          UUID,                      // 公開用 UUID（店詳細の URL に使う）
  googlePlaceId: Option[String],            // Google の店への参照（手動登録なら None）
  name:          String,                    // 店名（会員が付けた呼び名。登録時に Google から転記して確定）
  updatedAt:     LocalDateTime = Now,       // データ更新日
  createdAt:     LocalDateTime = Now        // データ作成日
) extends EntityModel[Id]

object Place:

  /** 店の識別子 */
  type   Id = Id.Repr
  object Id extends Entity.Id[Long]

  type   UUID = UUID.Repr
  object UUID extends Entity.Id[String]
```

**ここで型が語っていること**
- `googlePlaceId` が `Option` なのは、API に無い店・社食・オフィス内の場所を手動登録できるから
- `uuid` が公開用、`id` は DB の中だけ。店詳細の URL は `uuid` で指す（→ [論点11](#論点11店にも公開用の識別子を持たせる)）

**型では守れない決めごと**
- `googlePlaceId` は（値があるとき）一意。同じ Google の店から 2 行作らない
- `name` は会員の入力データ（登録時に Google の名前を転記して確定。共有マスタなので誰でも直せる）。住所・営業時間などは保存せず、表示のたびに Google から取得する（→ [論点4](#論点4店名は写しではなく会員が付けた呼び名として持つ)）

**保存されるデータの例**

| googlePlaceId | name |
|---|---|
| ChIJ…abc | 麺屋いちば |
| — | 社食（本社 3F） |

### `PlaceReview` — レビュー

```scala
case class PlaceReview(
  id:        Option[Id],                    // 管理 ID（永続化前は None）
  placeId:   Place.Id,                      // どの店か
  memberId:  Member.Id,                     // 書いた人（実名で表示する）
  star:      Short,                         // 星（1〜5）
  comment:   String,                        // 一言（時間帯・行列もここに自由に書く）
  updatedAt: LocalDateTime = Now,           // データ更新日
  createdAt: LocalDateTime = Now            // データ作成日
) extends EntityModel[Id]

object PlaceReview:

  /** レビューの識別子 */
  type   Id = Id.Repr
  object Id extends Entity.Id[Long]
```

**ここで型が語っていること**
- イベントへの参照を持たない。レビューは店と会員だけの世界で完結する（確定済みの要件）

**型では守れない決めごと**
- `star` は 1〜5（→ [論点3](#論点3成立人数と星を値オブジェクトにしない)）
- 編集・削除できるのは本人だけ。1 人 1 店に複数書ける（一意制約なし）
- 削除は物理削除（行を消す）。訪問日は持たない——`createdAt` が実質の訪問日（→ [論点6](#論点6レビューに足さなかったもの)）

**保存されるデータの例**

| placeId | memberId | star | comment |
|---|---|---|---|
| 5 | 24 | 4 | 11:45 なら並ばず入れた。味玉推し |

## 区分値の考え方

区分値は `Event.Status` の 1 つだけ。**正が「生きている・進んでいる」、負が「途中で終わった」**。

| 値 | code | 意味 | 一覧に出るか |
|---|---|---|---|
| `IS_OPEN` | 1 | 募集中 | 出る（進捗は分数「2/4」） |
| `IS_CONFIRMED` | 2 | 成立。名前が開示されている | 出る（進捗は実数「行く人 5 人」。分数は出さない） |
| `IS_FINISHED` | 3 | 終了（集合日時を過ぎた） | 当日中だけ出る。翌日から参加者の履歴のみ |
| `IS_FAILED` | -1 | 不成立（締切までに届かず） | 出ない。各自の履歴のみ |
| `IS_CANCELED` | -2 | 取り消し | 出ない。成立後の取り消しは参加者の履歴に見える |

業務には「成立前の取り消し」「成立後の取り消し」の 2 語があるが、**区分値は `IS_CANCELED` 1 つ**。どちらだったかは `confirmedAt` の有無で読む（呼称の数と区分値の数は一致しなくてよい）。

## エンティティをまたぐルール

- **成立判定**: 参加数（`EventEntry` の行数）が `minEntries` に達したら成立。立案者の 1 行を含めて数える。判定は**参加を受け付けた処理の中で即時**（バッチは締切→不成立、集合日時→終了、期限切れセッションの掃除だけ）
- **成立は 1 回だけ**: 成立にする更新に「まだ `IS_OPEN` である」条件を付け、**1 行更新できたときだけ Slack に流す**。同時参加でも成立後の再到達でも二重に起きない
- **参加できる条件**: `(IS_OPEN かつ 現在 < closeAt)` または `(IS_CONFIRMED かつ 現在 < startAt)`。状態だけでは足りない（バッチが動く前の数十秒に締切後の参加が通る）。バッチ側は `closeAt ≦ 現在`・`startAt ≦ 現在` を拾うので、隙間も重複もない
- **参加の取り消し**（行の削除）も同じ 2 状態に限る。ただし**立案者が自分の参加だけを消せるのは `IS_CONFIRMED` のときだけ**（募集中に消えると立案者不在のイベントが募集を続ける）
- **名簿は表示名の五十音順**（`Member.nameKana` で並べる）。参加順・ID と相関する並びにしない。名簿は都度計算＝いま行く人なので、開示後に取り消した人は消える
- **保存しないもの**: 進捗・本人の履歴・店の平均点・提案 3 件はすべて都度計算する。進捗は募集中だけ分数、成立後は実数
- **提案の並び順**: `(3 × 全店の平均星 + 星の合計) ÷ (3 + レビュー件数)` の降順。同点は件数の多い順→新しい順。3 件に足りない分はレビューの無い登録済みの店 → Google（会社周辺・評価順）で埋める

## 入力値の範囲

DDL を書くときに必ず要る値。ここに 1 表で置く。

| 対象 | 範囲・規則 | 備考 |
|---|---|---|
| `Event.title` | 1〜60 文字・改行不可 | 場所もここに書くので実質の「場所欄」 |
| `Event.startAt` | 現在より後、1 年先まで | 上限が無いと誤入力の年が残る |
| `Event.closeAt` | 現在 < `closeAt` ≦ `startAt`（既定は `startAt`） | 下限が無いと参加できないイベントが作れる |
| `Event.minEntries` | 2〜50（既定 4） | 既定は要求文の「4 人集まったら行く」 |
| `Event.code` | `SecureRandom` 128bit 相当（URL-safe な 22 文字）・一意・衝突時は再生成 | 推測されると総当たりで列挙できる |
| `Member.uuid` | UUIDv4 | 連番を外に晒さない |
| `Member.email` | 一意 | ログイン ID |
| `Member.name` | 1〜20 文字・一意にしない・変更できる | 同姓同名がありうる |
| `Member.nameKana` | 1〜40 文字・全角カタカナのみ・必須 | ひらがな混在は並びが壊れる |
| `PlaceReview.star` | 1〜5 | — |
| `PlaceReview.comment` | 0〜200 文字・改行不可・任意 | 星だけでも出せる |
| `Place.uuid` | UUIDv4 | 連番を外に晒さない |
| `Place.name` | 1〜40 文字。Google の店を指していない行は一意 | 表記揺れは止まらない |
| すべての日時 | JST 固定で保存・比較。日付の境界は JST の 00:00 | 現在時刻は注入可能な `Clock` から取る |

## 置き場所（コンテキスト）

**判断:** `member`（Member / MemberPassword / MemberSession）、`event`（Event / EventEntry）、`place`（Place / PlaceReview）。`common` は作らない。

**理由:** 変更の理由・ライフサイクル・公開性が 3 つとも違う（詳細は [02_analysis.md](./02_analysis.md)）。common に入れてよい 3 条件を満たすデータが無い。

---

# 付録A：今回やらないこと

- 定員（上限人数）。あと乗り自由と整合させる
- 公開後のイベントの編集（取り消して立て直す）
- グループ分け・マルチテナント（1 社・全員に見える）
- 登録制限（メールドメイン制限・招待制）。URL が出るのは社内 Slack だけで、混入が起きたら DB 直接対応で止める
- 通報・強制削除・管理者ロール・管理画面（設定は設定ファイル）
- 参加者からの手動の店提案（提案はアプリの自動のみ）
- 立案時に店を選ぶ機能（場所を伝えたいときはタイトルに書く → 論点5）
- レビューとイベントの紐づけ
- 会員個人の位置情報の利用
- 集計・レポート機能
- 参加の取り消し履歴（取り消したという記録は残さない → 論点2）
- レビューへの写真の添付（要求は「星と一言」。画像ストレージ一式が増えるため作らない）
- 重複した店を 1 つにまとめる操作（登録前に「これですか？」と聞くところまでで止める）
- 行き先の記録（決まった店をイベントに残さない。当日に集合場所が分からず迷う事例が出たら見直す）
- 個人向けのプッシュ通知（一覧の本人向けの印と履歴の入口で足りる）
- bot 投稿の再送（3 回試して諦める。投稿状態の列もバッチも持たない）
- 集合場所のメモ欄（タイトルに書く）
- 閲覧数の計測（押していない人数が圧の材料になりうる）
- アプリ名・アイコン・ホーム画面からの起動先の決定（入口の制作物は実装段階で決める）

# 付録B：判断の記録

設計の途中で判断が必要になった点を、理由とともに残します。検討の経緯そのものは [memo.md](./memo.md) に置きます。

### 論点1：取り消しは 1 つの状態で表す
**判断：`IS_CANCELED` は 1 つ。成立していたかは `confirmedAt` の有無で読む。**

見え方は「成立前＝本人の履歴のみ／成立後＝参加者にも見える」と違うので区別は必要ですが、成立の事実はすでに `confirmedAt` として持っています。状態を 2 値に割ると、同じ事実を 2 か所に書くことになります。代わりに `state` だけでは成立後の取り消しか分からず、必ず `confirmedAt` とセットで読む必要があります。

### 論点2：参加の取り消しは行を消す
**判断：`EventEntry` に状態を持たせず、物理削除する。**

「押したものが履歴に残る」は、いま押している参加＋終了・不成立時点で残っていた参加で満たせます。取り消した事実まで残すと「誰が断ったか」に近い記録が生まれ、**不参加の記録は存在しない**という確定済みの原則と衝突します。消えて困る人がいない、数少ないデータです。ただしバックアップから戻すと取り消した参加が復活するので、復旧手順には是正を含めます。

### 論点3：成立人数と星を値オブジェクトにしない
**判断：`minEntries: Short`（2〜50）と `star: Short`（1〜5）は素の型で持つ。**

どちらも使う場所が 1 か所で、型を作っても守れる範囲が広がりません（教材の `Money` / `Quantity` はコンテキスト横断で頻出だから型にした）。集計や比較が複数箇所に散り始めたら型に昇格させます。

### 論点4：店名は「写し」ではなく「会員が付けた呼び名」として持つ
**判断：`Place` は `googlePlaceId` と `name` だけを持ち、`name` は会員の入力データとして扱う。**

登録時に Google の検索結果を選ぶと店名がフォームに転記され、会員がそのまま（または直して）確定します。place_id だけで運用する案は成立しません——レビュー一覧を開くたびに店の数だけ Google を呼ぶことになり、障害時は店名すら出ず、そして**閉店すると place_id が無効になることがあり「レビューは無期限に残す」を Google のデータでは守れない**（5 年後のレビューが名無しになる）。表示に要る最低限は自前で持つしかありません。

「写し」ではなく「会員の入力」とすることで、規約上の性格が「Google コンテンツのキャッシュ（期限つき）」から「place_id を参照するユーザー生成データ」に変わります。代わりに Google 上の正式名とはズレうること、そして**変更前の値を残さないので、このアプリで唯一 DB 直接対応でも復旧できないデータ**になることを受け入れます。公開サービス化するときは法務レビューが必須です。

### 論点5：イベントは場所を持たない
**判断：`Event` から店への参照も、場所の自由記述も持たない。**

店の「決定」を記録しないと確定済みなので、参照を持つと event → place の依存が 1 本増えるだけです。自由記述は雑な記入が溜まります。場所を伝えたいときはタイトルに書きます（「今日ランチ@サイゼ」）。「イベントから店のレビューを開きたい」という要望が実際に出たら、自由記述ではなく参照を足します。

### 論点6：レビューに足さなかったもの
**判断：`placeId` / `memberId` / `star` / `comment` の 4 つで確定。**

- **訪問日**: 「食べたらそのまま書ける」が本線なので投稿日≒訪問日。任意にしても大半が空欄になる
- **編集済みフラグ**: `updatedAt ≠ createdAt` で計算できる
- **店名のスナップショット**: 店は消さない共有マスタなので参照が切れない
- **論理削除**: 本人が消すものは「消えて困る人がいない」。全クエリに除外条件が要る方が事故る

### 論点7：`Member.Status` を残す
**判断：無効化の操作・画面は作らないが、区分値は持つ。**

要件に退会・無効化はありません。それでも残すのは「問題が起きたら DB 直接対応」という確定済みの運用の**受け皿**が必要だからです——`state` を 1 行変えればログインを止められます。無効になった会員の参加・レビューはそのまま残します。

### 論点8：成立人数の下限は 2 のまま
**判断：2 人（立案者込み）を許容する。2 人で成立すると相手からは立案者が確定する。**

立案者は自分も参加するので、**成立名簿には必ず立案者が含まれます**——特定の確率は元から 1/n で、2 はその端にあたるだけです。そして匿名が本当に守っているのは**不成立の局面**で、そこでは一覧からも消え、URL を踏んでも一文しか出ず、Slack にも流れません。成立とは「相手が行くと言った」状態なので、誰の誘いかが分かるのはお礼が言える方向に働きます。下限を 3 に上げると「一人来てくれれば行くランチ」が作れず、要求の中心にある軽さを失います。

### 論点9：開示後の名簿からの離脱は見えるままにする
**判断：名簿は都度計算（いま行く人）のままにし、名前が消えることは見えると要件に書く。**

隠すには成立時点の名簿を固定して保存するしかなく、「消さない設計だからスナップショットは不要」という判断を崩す上に、**名簿にいるのに来ない人**が生まれて集合の役に立ちません。名簿の役割は「いま誰と行くか」です。代わりに「入って抜けた」が見えるので、参加を押す前に伝えて驚きにしないようにします。

### 論点10：匿名が守られる範囲を「表示とログ」と定義する
**判断：アプリのレスポンスとログまでを範囲とし、DB 権限を持つ人には成立しないと明記する。**

匿名を表示ルールで守ると決めた以上、範囲を書かないと「立案者 ID が混ざっていない」ことを何と比べて判定するのかが決まりません。レスポンスは載せてよいフィールドの一覧（[memo.md](./memo.md)）が正となり、キー集合の突き合わせで検証できます。ログ側は、会員識別子とセッショントークンを出さない・リクエストボディを残さない・例外通知に会員 ID を含めない、の 3 つで守ります。監査要件が生まれたら「誰が何を見たか」を残す設計になり、この定義ごと見直しになります。

### 論点11：店にも公開用の識別子を持たせる
**判断：`Place` に `uuid` を足し、店詳細の URL はこれで指す。**

持たない案も成り立ちました。会員とイベントが公開用の識別子を持つ理由は「匿名性」と「推測されると総当たりで列挙できる」の 2 つで、店にはどちらも当てはまりません——レビューは実名、店リストはログイン不要で全件見えるので、連番で辿られても正面から見えるものと同じです。それでも持たせたのは、3 エンティティのうち 1 つだけ連番を晒す形が、読む人に「なぜここだけ違うのか」を毎回考えさせるからです。代償は列とマイグレーションが 1 本ずつ増えること。形式はイベントの `code`（推測されない 22 文字）ではなく会員と同じ UUIDv4 にしました——強い秘匿が要る場面ではなく、揃える先は同じ理由（連番を晒さない）を持つ会員だからです。判断が変わる条件：店を非公開にする運用や、URL を知っている人にだけ見せる要求が出たとき。そのときは `code` 側の形式に寄せます。

# 付録C：画面と遷移

図は作らず、画面の一覧で持つ。SvelteKit のルートと、どの画面がどのエンティティを読むかがこれで決まる。

| 画面 | ログイン | 表示するもの | 遷移元 → 先 |
|---|---|---|---|
| イベント一覧 | 不要 | Event（集合日時の昇順・終了当日は末尾）＋参加数。本人の印はログイン時のみ | 入口（Slack の URL・ホーム画面）→ 詳細・作成・店リスト・履歴 |
| イベント詳細（募集中） | 不要 | Event＋参加数（分数） | 一覧・Slack の URL → 参加（ログイン）・取り消し |
| イベント詳細（成立） | 不要 | Event＋名簿（表示名・五十音順）＋実数＋提案 3 件 | 同じ → 参加・取り消し・店詳細 |
| イベント詳細（終了当日） | 不要 | 成立と同じ＋「終了」ラベル | 一覧 → 店詳細 |
| 最小表示 | 不要 | 「このイベントは終了しました」＋履歴への 1 行 | URL 直踏み → ログイン |
| イベント作成 | 必須 | 入力（前回の値を初期値）→ **確認** → 公開 | 一覧 → 詳細 |
| 履歴 | 必須 | 自分が立てた・押したイベント／自分のレビュー（1 タブ） | 一覧 → 詳細・レビュー編集 |
| 店リスト | 不要 | 会社周辺の Google 検索結果＋登録済みの店とレビュー | 一覧・イベント詳細 → 店詳細 |
| 店詳細 | 不要 | Place＋PlaceReview 一覧（実名） | 店リスト → レビュー投稿・店名の編集 |
| レビュー投稿・編集 | 必須 | 星と一言。同じ店に直近の自分のレビューがあれば「前回のを直す／新しく書く」を聞く | 店詳細・履歴 → 店詳細 |
| 店の手動登録 | 必須 | 名前の入力。前に既存の店を前方一致で出して「これですか？」 | レビュー投稿 → レビュー投稿 |
| ログイン | — | メール＋パスワード | 参加・作成・レビュー投稿 → 元の画面 |
| 会員登録 | — | メール・パスワード・表示名・**表示名の読み** | ログイン → 一覧（初回は匿名の範囲の説明を 1 回出す） |
